---
name: "Brainstorming"
description: "Explore user intent, requirements and design before implementation"
category: Workflow
tags: [brainstorming, design, exploration]
---

Invoke the `brainstorming` skill to explore the user's idea before writing any code.

Use the Skill tool with `skill: "brainstorming"` and pass the user's argument (if any) as `args`. Follow the skill's instructions exactly — do not implement anything until the user approves a design.
